1. The script reads the data from the test folder and train folder separately and joins them with their matching subjects.
2. Activities are converted to factors with their corresponding numbers
3. A tidy data set is created with each activity and subject as a group. Then the average of all other variables is computed for each group combination.